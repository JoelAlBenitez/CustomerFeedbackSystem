{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//secrets": [
    "Both values below are deliberately empty: this file is versioned, so it holds structure only.",
    "dotnet user-secrets set \"ConnectionStrings:CustomerReviewSystemData\" \"<OLTP connection string>\" --project CustomerFeedbackSystem.OLAP.Api",
    "dotnet user-secrets set \"Security:ApiKey\" \"<generated key>\" --project CustomerFeedbackSystem.OLAP.Api",
    "Generate the key, do not invent it: [Convert]::ToBase64String((1..32|%{Get-Random -Max 256}))",
    "The same key goes into the Worker under Sources:Api:ApiKey."
  ],
  "ConnectionStrings": {
    "CustomerReviewSystemData": ""
  },
  "Security": {
    "ApiKey": ""
  },

  "Paging": {
    "DefaultPageSize": 500,
    "MaxPageSize": 1000
  }
}
